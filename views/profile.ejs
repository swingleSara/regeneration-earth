<%- include('partials/header') -%>
<div class="container">
  <div class="row mt-1">
    <div id="left-col" class="col-6 fixed-top bkg">
      <div>
          <p><strong>User Name</strong>: <%= user.userName %></p>
          <p><strong>Email</strong>: <%= user.email %></p>
          <a href="/logout" class="col-3 btn">Logout</a>
      </div>
      <div class="mt-3 mb-3 fnt-wht">
        <h2>Add a file</h2>
        <form action="/file/createFile" enctype="application/x-www-form-urlencoded" method="POST">
          <div class="mb-3">
            <label for="client" class="form-label">Client</label>
            <input type="text" class="form-control" id="client" name="client">
          </div>
          <div class="mb-3">
            <label for="link" class="form-label">Link</label>
            <input type="text" class="form-control" id="link" name="link">
          </div>
          <div class="mb-3">
              <label for="word" class="form-label">Word</label>
              <input type="text" class="form-control" id="word" name="word">
          </div>
          <button type="submit" class="btn" value="Upload">Submit</button>
        </form>
      </div>
    </div>
    <div class="offset-sm-6">
      <ul class="list-unstyled">
        <% for(var i=0; i<files.length; i++) {%>
          <li class="col-6 justify-content-between mt-1">
            <a href="/file/<%= files[i]._id%>" class="link-info">
              <span><%= files[i].client %>  <%= files[i].createdAt.toDateString() %></span>
            </a>
          </li>
        <% } %>
      </ul>  
    </div>  
  </div>
</div>
<%- include('partials/footer') -%>